fragment CharacterDetails on Character {
    name
}

query FetchCharacters($page: Int = 1) {
    characters(page: $page) {
        info {
            count
        }
        results {
            ...CharacterDetails
        }
    }
}
